import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import MarketplaceHeader from "@/app/components/marketplace/MarketplaceHeader";
import MobileBottomNav from "@/app/components/marketplace/MobileBottomNav";
import { getJob, getMarketplaceJobDisplayTitle, getService } from "@/app/data/marketplace";
import { changeMarketplaceProvider, chooseMarketplaceQuote, confirmMarketplaceCompletion, reportMarketplaceCompletionIssue, startCustomerConversation, submitMarketplaceOffer, submitMarketplaceReview } from "@/app/jobs/actions";
import { createMarketplaceCheckout } from "@/app/jobs/payment-actions";
import { getOperationalMarketplaceProvider } from "@/lib/marketplace/provider-access";
import { createSupabaseAdminClient } from "@/lib/supabase/admin";
import { createSupabaseServerClient } from "@/lib/supabase/server";
import { ACTIVE_MARKETPLACE_OFFER_STATUSES, formatMarketplaceAmount, formatMarketplaceSchedule, getCustomerJobLifecycleState, getMarketplacePaymentState, getMarketplaceQuoteProviderId, isPartialMarketplaceRefund } from "@/lib/marketplace/customer-job-state";
import { formatMarketplaceProviderName } from "@/lib/marketplace/presentation";
import { resolveProviderPhotoUrl } from "@/lib/marketplace/provider-photo";
import { resolveMarketplaceJobState } from "@/lib/marketplace/booking-state";
import { reconcileMarketplacePaymentOnReturn } from "@/lib/server/marketplace-payment-finalization";
import CustomerJobManageMenu from "@/app/jobs/CustomerJobManageMenu";
import ProviderAvatar from "@/app/components/marketplace/ProviderAvatar";

export const metadata: Metadata = { title: "Quickola job", robots: { index: false, follow: false } };

function relativePostedAt(value: string) { const seconds = Math.max(0, Math.floor((Date.now() - new Date(value).getTime()) / 1000)); if (seconds < 60) return "just now"; if (seconds < 3600) return `${Math.floor(seconds / 60)} minute${Math.floor(seconds / 60) === 1 ? "" : "s"} ago`; if (seconds < 86400) return `${Math.floor(seconds / 3600)} hour${Math.floor(seconds / 3600) === 1 ? "" : "s"} ago`; if (seconds < 172800) return "yesterday"; return new Intl.DateTimeFormat("en-GB", { dateStyle: "medium" }).format(new Date(value)); }

export default async function JobPage({ params, searchParams }: { params: Promise<{ token: string }>; searchParams: Promise<{ offered?: string; selected?: string; error?: string; payment?: string }> }) {
  const { token } = await params;
  const { offered, error: offerError, payment } = await searchParams;
  const admin = createSupabaseAdminClient();
  const { data, error } = await admin.from("marketplace_jobs").select("id,customer_id,service,service_subtype,pricing_answers,postcode,requested_timing,optional_note,budget_amount,status,created_at").eq("public_token", token).maybeSingle();
  if (error || !data) notFound();
  const supabase = await createSupabaseServerClient();
  const { data: { user } } = await supabase.auth.getUser();
  const customer = user ? (await admin.from("marketplace_customers").select("id").eq("auth_user_id", user.id).maybeSingle()).data : null;
  const isOwner = Boolean(customer && customer.id === data.customer_id);
  const provider = user && !isOwner ? await getOperationalMarketplaceProvider() : null;
  const service = getService(data.service);
  const selectedJob = getJob(data.service, data.service_subtype);
  const selected = false;
  const customerVisibleQuoteStatuses = [...ACTIVE_MARKETPLACE_OFFER_STATUSES, "declined"] as const;
  const { data: quoteRows } = await admin.from("marketplace_quotes").select("id,amount_pence,availability_text,message,status,provider_id,bidder_user_id,scheduled_date,arrival_window_start,arrival_window_end").eq("job_id", data.id).in("status", customerVisibleQuoteStatuses).order("created_at", { ascending: true });
  const quoteProviderIds = [...new Set((quoteRows || []).map(getMarketplaceQuoteProviderId).filter(Boolean))];
  const { data: quoteProviderProfiles } = quoteProviderIds.length ? await admin.from("marketplace_providers").select("user_id,display_name,business_name,profile_photo_url").in("user_id", quoteProviderIds) : { data: [] };
  const resolvedQuoteProfiles = await Promise.all((quoteProviderProfiles || []).map(async (profile) => ({ ...profile, profile_photo_url: await resolveProviderPhotoUrl(admin, profile.profile_photo_url) })));
  const quoteProfilesById = new Map(resolvedQuoteProfiles.map((profile) => [profile.user_id, profile]));
  const quotes = (quoteRows || []).map((quote) => ({ ...quote, marketplace_providers: quoteProfilesById.get(getMarketplaceQuoteProviderId(quote) || "") || null }));
  const { data: conversations } = isOwner ? await admin.from("marketplace_conversations").select("id,provider_id,bidder_user_id,created_at").eq("job_id", data.id) : { data: [] };
  const conversationByProvider = new Map((conversations || []).map((item) => [item.provider_id || item.bidder_user_id, item.id]));
  const conversationIds = (conversations || []).map((item) => item.id);
  const [{ data: conversationProfiles }, { data: conversationMessages }] = isOwner && conversationIds.length ? await Promise.all([
    admin.from("marketplace_providers").select("user_id,display_name,business_name,profile_photo_url").in("user_id", [...new Set((conversations || []).map((item) => item.provider_id || item.bidder_user_id).filter(Boolean))]),
    admin.from("marketplace_messages").select("conversation_id,body,created_at").in("conversation_id", conversationIds).order("created_at", { ascending: false }),
  ]) : [{ data: [] }, { data: [] }];
  let conversationSummaries = await Promise.all((conversations || []).map(async (conversation) => {
    const providerId = conversation.provider_id || conversation.bidder_user_id;
    const profile = (conversationProfiles || []).find((item) => item.user_id === providerId);
    const latest = (conversationMessages || []).find((item) => item.conversation_id === conversation.id);
    const quote = (quoteRows || []).find((item) => getMarketplaceQuoteProviderId(item) === providerId);
    return { id: conversation.id, providerName: formatMarketplaceProviderName(profile?.business_name || profile?.display_name), providerPhotoUrl: await resolveProviderPhotoUrl(admin, profile?.profile_photo_url), latestBody: latest?.body || null, latestAt: latest?.created_at || conversation.created_at, hasQuote: Boolean(quote) };
  }));
  let { data: bookings } = isOwner ? await admin.from("marketplace_bookings").select("id,quote_id,conversation_id,amount_pence,payment_status,stripe_checkout_session_id,paid_at,status,completion_status,payout_hold_status,payout_hold_reason,refunded_amount_pence,scheduled_date,arrival_window_start,arrival_window_end,provider_arrived_at,provider_finished_at,customer_completed_at,provider_id,marketplace_providers(display_name,business_name,profile_photo_url)").eq("job_id", data.id) : { data: [] };
  const { data: jobPhotos } = isOwner ? await admin.from("marketplace_job_photos").select("id,storage_path").eq("job_id", data.id).order("created_at", { ascending: true }) : { data: [] };
  const photos = await Promise.all((jobPhotos || []).map(async (photo) => ({ id: photo.id, url: (await admin.storage.from("marketplace-job-photos").createSignedUrl(photo.storage_path, 3600)).data?.signedUrl || null })));
  const liveBooking = (bookings || [])[0] || null;
  const resolvedState = resolveMarketplaceJobState({ job: data, booking: liveBooking, quotes: quotes || [] });
  const acceptedQuote = resolvedState.currentQuoteId ? quotes?.find((quote) => quote.id === resolvedState.currentQuoteId) : undefined;
  if (acceptedQuote) conversationSummaries = [];
  const selectedBooking = liveBooking;
  if (payment === "success" && isOwner && selectedBooking?.id) {
    try {
      if (await reconcileMarketplacePaymentOnReturn(admin, selectedBooking.id)) {
        const refreshed = await admin.from("marketplace_bookings").select("id,quote_id,conversation_id,amount_pence,payment_status,stripe_checkout_session_id,paid_at,status,completion_status,payout_hold_status,payout_hold_reason,refunded_amount_pence,scheduled_date,arrival_window_start,arrival_window_end,provider_arrived_at,provider_finished_at,customer_completed_at,provider_id,marketplace_providers(display_name,business_name,profile_photo_url)").eq("job_id", data.id);
        if (!refreshed.error) bookings = refreshed.data;
      }
    } catch (error) { console.error("[marketplace-payment] success-return reconciliation failed", { bookingId: selectedBooking.id, reason: error instanceof Error ? error.message : "unknown" }); }
  }
  const acceptedBooking = liveBooking;
  const { data: activeCompletionDispute } = isOwner && acceptedBooking ? await admin.from("marketplace_disputes").select("id").eq("booking_id", acceptedBooking.id).in("status", ["open", "in_review"]).maybeSingle() : { data: null };
  const lifecycle = isOwner ? getCustomerJobLifecycleState({ offerCount: quotes?.length || 0, acceptedQuote, booking: acceptedBooking, hasActiveDispute: Boolean(activeCompletionDispute) }) : null;
  if (acceptedBooking && isPartialMarketplaceRefund(acceptedBooking) && acceptedBooking.payment_status === "partially_refunded") acceptedBooking.payment_status = "paid";
  const title = getMarketplaceJobDisplayTitle(data.service, data.service_subtype, selectedJob?.name || service?.name || data.service);
  const acceptedProfile = Array.isArray(acceptedQuote?.marketplace_providers) ? acceptedQuote.marketplace_providers[0] : acceptedQuote?.marketplace_providers;
  const providerName = formatMarketplaceProviderName(acceptedProfile?.business_name || acceptedProfile?.display_name);
  const acceptedProviderPhotoUrl = await resolveProviderPhotoUrl(admin, acceptedProfile?.profile_photo_url);
  const acceptedProviderId = acceptedQuote?.provider_id || acceptedQuote?.bidder_user_id || null;
  const schedule = formatMarketplaceSchedule(acceptedBooking || acceptedQuote || null);
  const review = acceptedBooking ? (await admin.from("marketplace_reviews").select("rating,review_text,created_at").eq("booking_id", acceptedBooking.id).maybeSingle()).data : null;
  const canManage = isOwner && (!acceptedQuote && ["posted", "finding_provider", "provider_available"].includes(data.status) || Boolean(acceptedBooking) && getMarketplacePaymentState(acceptedBooking) !== "paid" && acceptedBooking?.status !== "cancelled");
  const paymentReturnPending = payment === "success" && acceptedBooking?.payment_status !== "paid";
  return <main className="min-h-screen bg-white text-[#061b3f]"><MarketplaceHeader /><section className="mx-auto max-w-3xl px-5 pb-[calc(7rem+env(safe-area-inset-bottom))] py-7 sm:px-8 sm:py-12 sm:pb-16"><Link href="/my-jobs" className="text-sm font-black text-[#167d3c]">← My jobs</Link><div className="mt-4 rounded-3xl border border-[#e7ebef] bg-white p-6 sm:p-9"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.16em] text-[#159548]">{isOwner ? "Your job" : "Local job"}</p><h1 className="mt-3 text-4xl font-black tracking-[-.06em]">{title}</h1><p className="mt-4 text-sm text-[#526078]">{data.postcode} · {data.requested_timing || "Flexible timing"}</p></div>{isOwner && <CustomerJobManageMenu token={token} canManage={canManage} note={data.optional_note} timing={data.requested_timing} budget={data.budget_amount} />}</div>{isOwner && !acceptedQuote && <Detail offerCount={quotes?.length || 0} createdAt={data.created_at} />}{isOwner && acceptedQuote && acceptedBooking && lifecycle && (getMarketplacePaymentState(acceptedBooking) === "paid" ? <BookingConfirmation token={token} jobId={data.id} booking={acceptedBooking} quote={acceptedQuote} lifecycle={lifecycle} providerName={providerName} providerId={acceptedProviderId} providerPhotoUrl={acceptedProviderPhotoUrl} schedule={schedule} review={review} /> : <BookingPanel token={token} jobId={data.id} booking={acceptedBooking} quote={acceptedQuote} lifecycle={lifecycle} providerName={providerName} providerId={acceptedProviderId} schedule={schedule} review={review} paymentReturnPending={paymentReturnPending} />)}{selected && <p className="mt-5 rounded-xl bg-[#eef8f1] p-4 font-bold text-[#167d3c]">Provider selected. Review the agreed price, then pay when you’re ready.</p>}{["stripe_config", "payment_setup", "payment"].includes(offerError || "") && <p className="mt-5 rounded-xl bg-red-50 p-4 font-bold text-red-800">Payment could not be started. Your accepted offer is safe. Please try again.</p>}{payment === "cancelled" && <p className="mt-5 rounded-xl bg-[#f7f8fa] p-4 font-bold text-[#526078]">Payment was cancelled. Your accepted offer is still available.</p>}{!isOwner && provider && <ProviderOfferForm token={token} offered={Boolean(offered)} error={offerError} />}{isOwner && <Offers quotes={quotes || []} token={token} jobId={data.id} conversationByProvider={conversationByProvider} selectedQuoteId={acceptedQuote?.id || null} paymentLocked={getMarketplacePaymentState(acceptedBooking) === "paid"} completed={lifecycle === "completed"} />}{isOwner && conversationSummaries.length > 0 && <ProviderConversations conversations={conversationSummaries} />}{isOwner && <JobDetails data={data} photos={photos} selected={Boolean(acceptedQuote)} paymentStatus={acceptedBooking?.payment_status} lifecycle={lifecycle} />}</div></section><div className="md:hidden"><MobileBottomNav /></div></main>;
}
function BookingConfirmation({ token, jobId, booking, quote, lifecycle, providerName, providerId, providerPhotoUrl, schedule, review }: { token: string; jobId: string; booking: { id: string; conversation_id?: string | null; amount_pence: number; refunded_amount_pence?: number | null; payment_status?: string | null; stripe_checkout_session_id?: string | null; status?: string | null; completion_status?: string | null; payout_hold_status?: string | null; scheduled_date?: string | null; arrival_window_start?: string | null; arrival_window_end?: string | null }; quote: { id: string; amount_pence: number }; lifecycle: ReturnType<typeof getCustomerJobLifecycleState>; providerName: string; providerId: string | null; providerPhotoUrl?: string | null; schedule: ReturnType<typeof formatMarketplaceSchedule>; review: { rating: number; review_text: string | null } | null }) {
  const paid = getMarketplacePaymentState(booking) === "paid";
  const partialRefund = isPartialMarketplaceRefund(booking);
  const remainingPaid = Math.max(0, Number(booking.amount_pence || quote.amount_pence) - Number(booking.refunded_amount_pence || 0));
  const messageHref = booking.conversation_id ? `/messages/${booking.conversation_id}` : null;
  const completionReady = lifecycle === "awaiting_customer_completion";
  return <section className="mt-7 rounded-2xl border border-[#cfe9d7] bg-[#f5fbf6] p-5 sm:p-6">
    <p className="text-xs font-black uppercase tracking-[.14em] text-[#167d3c]">{paid ? "Booking confirmed" : "Payment processing"}</p>
    <h2 className="mt-2 text-2xl font-black">{paid ? `${providerName} is booked for your job` : "We’re confirming your booking"}</h2>
    <div className="mt-5 flex items-center gap-3">{providerPhotoUrl ? <img src={providerPhotoUrl} alt="" className="h-14 w-14 rounded-full object-cover" /> : <div className="flex h-14 w-14 items-center justify-center rounded-full bg-[#dff2e4] text-xl font-black text-[#167d3c]" aria-hidden="true">{providerName.slice(0, 1).toUpperCase()}</div>}<div><p className="font-black">{providerName}</p>{providerId && <Link href={`/providers/${providerId}?job=${jobId}&offer=${quote.id}&returnTo=/jobs/${token}`} className="mt-1 inline-flex text-sm font-black text-[#167d3c]">View profile</Link>}</div></div>
    <div className="mt-5 grid gap-2 border-t border-[#dce7df] pt-4 text-sm"><p><span className="font-black">Arrival time</span> · {schedule.dateLabel ? `${schedule.dateLabel} · ${schedule.timeLabel}` : booking.arrival_window_start ? schedule.timeLabel : `To be arranged with ${providerName}`}</p><p><span className="font-black">Paid</span> · {paid ? formatMarketplaceAmount(booking.amount_pence || quote.amount_pence) : "Payment being confirmed"}</p>{partialRefund && <p className="font-bold text-[#795b13]"><span className="font-black">{lifecycle === "completed" ? "Issue resolved by Quickola" : "Partial refund issued"}</span> · {formatMarketplaceAmount(booking.refunded_amount_pence)} refunded · {formatMarketplaceAmount(remainingPaid)} remains paid{lifecycle === "completed" ? " · Remaining booking amount settled with the provider" : ""}</p>}</div>
    {messageHref && <Link href={messageHref} className="mt-5 inline-flex min-h-11 w-full items-center justify-center rounded-xl bg-[#167d3c] px-4 font-black text-white">Message {providerName}</Link>}
    {completionReady && <form action={confirmMarketplaceCompletion} className="mt-5 grid gap-3 border-t border-[#dce7df] pt-5"><p className="font-black">Was the work completed?</p><label className="grid gap-1 text-sm font-bold" htmlFor={`rating-${booking.id}`}>Your rating<select id={`rating-${booking.id}`} name="rating" required defaultValue="" className="min-h-11 rounded-xl border border-[#dbe1ea] bg-white px-3"><option value="" disabled>Choose 1–5 stars</option>{[5, 4, 3, 2, 1].map((value) => <option key={value} value={value}>{"★".repeat(value)} ({value})</option>)}</select></label><label className="grid gap-1 text-sm font-bold" htmlFor={`review-${booking.id}`}>Review <span className="font-normal text-[#657089]">(optional)</span><textarea id={`review-${booking.id}`} name="review" maxLength={1000} placeholder="Tell us about the work" className="min-h-20 rounded-xl border border-[#dbe1ea] p-3" /></label><input type="hidden" name="token" value={token} /><input type="hidden" name="bookingId" value={booking.id} /><button className="min-h-11 rounded-xl bg-[#23a955] px-5 font-black text-[#061b3f]">Confirm completion &amp; submit review</button></form>}
    {completionReady && <form action={reportMarketplaceCompletionIssue} className="mt-3 grid gap-2"><input type="hidden" name="token" value={token} /><input type="hidden" name="bookingId" value={booking.id} /><input type="hidden" name="reasonCode" value="customer_completion_issue" /><textarea name="reasonText" required maxLength={2000} placeholder="Describe the problem" className="min-h-20 rounded-xl border border-[#dbe1ea] p-3" /><button className="min-h-11 rounded-xl border border-[#dbe1ea] px-5 font-black">Report a problem</button></form>}
    {lifecycle === "completion_issue_reported" && <div className="mt-5 border-t border-[#dce7df] pt-5"><p className="font-black">Your completion confirmation is paused</p><p className="mt-2 text-sm font-bold text-[#526078]">We’re reviewing the problem reported for this booking. Completion and payout remain on hold until it is resolved.</p></div>}
    {lifecycle === "completed" ? <div className="mt-6 border-t border-[#dce7df] pt-5"><p className="text-xs font-black uppercase tracking-[.14em] text-[#167d3c]">Completed</p><p className="mt-2 font-black">Job completed with {providerName}</p><p className="mt-2 text-sm font-bold text-[#526078]">Paid · {formatMarketplaceAmount(booking.amount_pence || quote.amount_pence)}</p></div> : <div className="mt-6 border-t border-[#dce7df] pt-5"><h3 className="font-black">What happens next?</h3><ol className="mt-3 grid gap-3 text-sm text-[#526078]"><li><span className="font-black text-[#061b3f]">1. Arrange the time</span><br />Message {providerName} to agree when they&apos;ll arrive.</li><li><span className="font-black text-[#061b3f]">2. Get the job done</span><br />Your provider completes the work.</li><li><span className="font-black text-[#061b3f]">3. Complete your job</span><br />Confirm completion when the work is finished.</li></ol></div>}
    {lifecycle === "completed" && review && <p className="mt-4 font-black text-[#167d3c]">Verified booking · Your review: {"★".repeat(review.rating)}</p>}
  </section>;
}
function BookingPanel({ token, jobId, booking, quote, lifecycle, providerName, providerId, schedule, review, paymentReturnPending = false }: { token: string; jobId: string; booking: { id: string; conversation_id?: string | null; amount_pence: number; refunded_amount_pence?: number | null; payment_status?: string | null; stripe_checkout_session_id?: string | null; status?: string | null; completion_status?: string | null; scheduled_date?: string | null; arrival_window_start?: string | null; arrival_window_end?: string | null }; quote: { id: string; amount_pence: number }; lifecycle: ReturnType<typeof getCustomerJobLifecycleState>; providerName: string; providerId: string | null; schedule: ReturnType<typeof formatMarketplaceSchedule>; review: { rating: number; review_text: string | null } | null; paymentReturnPending?: boolean }) {
  const paymentState = getMarketplacePaymentState(booking);
  const paid = paymentState === "paid";
  const refundState = lifecycle === "refunded" || lifecycle === "refund_pending";
  const partialRefundState = isPartialMarketplaceRefund(booking);
  const messageHref = booking.conversation_id ? `/messages/${booking.conversation_id}` : null;
  if (refundState) return <section className="mt-7 rounded-2xl border border-[#eadfbf] bg-[#fffaf0] p-5"><p className="text-xs font-black uppercase tracking-[.14em] text-[#795b13]">{lifecycle === "refunded" ? "Refunded" : "Refund processing"}</p><h2 className="mt-2 text-2xl font-black">{lifecycle === "refunded" ? "Your payment was refunded" : "Your refund is being processed"}</h2><p className="mt-3 text-sm font-bold text-[#526078]">Completion and provider payout remain unavailable for this booking.</p></section>;
  if (partialRefundState) return <section className="mt-7 rounded-2xl border border-[#eadfbf] bg-[#fffaf0] p-5"><p className="text-xs font-black uppercase tracking-[.14em] text-[#795b13]">Partial refund issued</p><h2 className="mt-2 text-2xl font-black">£{(Number(booking.refunded_amount_pence || 0) / 100).toFixed(2)} refunded · {lifecycle === "completion_issue_reported" ? "Issue being reviewed" : "Booking remains paid"}</h2><p className="mt-3 text-sm font-bold text-[#526078]">£{(Math.max(0, Number(booking.amount_pence || quote.amount_pence) - Number(booking.refunded_amount_pence || 0)) / 100).toFixed(2)} remains paid. Completion and provider payout remain unavailable while the issue or payout hold is unresolved.</p></section>;
  return <section className="mt-7 rounded-2xl border border-[#dce7df] bg-[#f5fbf6] p-5"><p className="text-xs font-black uppercase tracking-[.14em] text-[#167d3c]">{lifecycle === "completed" ? "Job complete ✓" : lifecycle === "awaiting_customer_completion" ? "Confirm job" : paid ? "Booking confirmed ✓" : "Provider selected"}</p><h2 className="mt-2 text-2xl font-black">{lifecycle === "booked" ? `${providerName} is coming` : lifecycle === "provider_on_the_way" ? `${providerName} is on the way` : lifecycle === "provider_arrived" ? `${providerName} has arrived` : lifecycle === "awaiting_customer_completion" ? `${providerName} has marked this job as finished.` : lifecycle === "completed" ? `${providerName} completed your job` : `You've chosen ${providerName}`}</h2>{providerId && <Link href={`/providers/${providerId}?job=${jobId}&offer=${quote.id}&returnTo=/jobs/${token}`} className="mt-2 inline-flex font-black text-[#167d3c]">View {providerName}&apos;s profile</Link>}{schedule.dateLabel && <p className="mt-4 font-black">{schedule.dateLabel}</p>}<p className="mt-1 font-bold text-[#526078]">{schedule.timeLabel}</p><p className="mt-3 font-black text-[#167d3c]">{paid ? `${formatMarketplaceAmount(booking.amount_pence || quote.amount_pence)} paid ✓` : `Agreed price · ${formatMarketplaceAmount(quote.amount_pence)}`}</p>{!paid && (paymentReturnPending ? <p className="mt-4 text-sm font-bold text-[#7a5a00]">Confirming your payment… Please refresh in a moment.</p> : <><p className="mt-4 text-sm text-[#526078]">Complete payment to confirm your booking.</p><form action={createMarketplaceCheckout} className="mt-4"><input type="hidden" name="token" value={token} /><input type="hidden" name="quoteId" value={quote.id} /><input type="hidden" name="returnTo" value={`/jobs/${token}`} /><button className="min-h-11 rounded-xl bg-[#23a955] px-5 font-black text-[#061b3f]">Confirm &amp; pay {formatMarketplaceAmount(quote.amount_pence)}</button></form></>)}{paid && <BookingProgress lifecycle={lifecycle} />}{lifecycle === "awaiting_customer_completion" && <div className="mt-4 flex flex-wrap gap-3"><form action={confirmMarketplaceCompletion}><input type="hidden" name="token" value={token} /><input type="hidden" name="bookingId" value={booking.id} /><button className="min-h-11 rounded-xl bg-[#23a955] px-5 font-black text-[#061b3f]">Yes, job complete</button></form><form action={reportMarketplaceCompletionIssue}><input type="hidden" name="token" value={token} /><input type="hidden" name="bookingId" value={booking.id} /><button className="min-h-11 rounded-xl border border-[#dbe1ea] px-5 font-black">There&apos;s a problem</button></form></div>}{paid && messageHref && <Link href={messageHref} className="mt-4 inline-flex min-h-11 items-center rounded-xl border border-[#167d3c] px-4 font-black text-[#167d3c]">Message {providerName}</Link>}{lifecycle === "completed" && !review && <form action={submitMarketplaceReview} className="mt-5 grid gap-3 border-t border-[#dce7df] pt-5"><p className="font-black">How did {providerName} do?</p><select name="rating" required defaultValue=""><option value="" disabled>Choose a rating</option>{[5, 4, 3, 2, 1].map((value) => <option key={value} value={value}>{value} stars</option>)}</select><textarea name="review" maxLength={1000} placeholder="Optional short review" className="min-h-20 rounded-xl border border-[#dbe1ea] p-3" /><input type="hidden" name="token" value={token} /><input type="hidden" name="bookingId" value={booking.id} /><button className="min-h-11 rounded-xl bg-[#061b3f] px-5 font-black text-white">Submit review</button></form>}{review && <p className="mt-4 font-black text-[#167d3c]">Your review: {"★".repeat(review.rating)} </p>}</section>;
}

function BookingProgress({ lifecycle }: { lifecycle: ReturnType<typeof getCustomerJobLifecycleState> }) {
  const complete = lifecycle === "completed";
  return <div className="mt-4 border-t border-[#dce7df] pt-4 text-sm font-bold text-[#526078]"><p className="text-[#167d3c]">✓ Booked</p><p className="mt-1">{complete ? "✓" : "○"} Complete</p></div>;
}

function ProviderOfferForm({ token, offered, error }: { token: string; offered: boolean; error?: string }) { return <section className="mt-8 border-t border-[#e9edf1] pt-7"><h2 className="text-2xl font-black">Send your offer</h2>{offered ? <p className="mt-3 rounded-xl bg-[#eaf8ee] p-4 font-bold text-[#167d3c]">Your offer has been sent.</p> : <form action={submitMarketplaceOffer} className="mt-4 grid gap-4"><label className="font-bold">Your price (£)<input name="amount" type="number" min="1" step="0.01" required className="mt-2 min-h-12 w-full rounded-xl border px-4" /></label><label className="font-bold">Service date<input name="scheduledDate" type="date" required className="mt-2 min-h-12 w-full rounded-xl border px-4" /></label><div className="grid grid-cols-2 gap-3"><label className="font-bold">From<input name="arrivalWindowStart" type="time" required className="mt-2 min-h-12 w-full rounded-xl border px-4" /></label><label className="font-bold">Until<input name="arrivalWindowEnd" type="time" required className="mt-2 min-h-12 w-full rounded-xl border px-4" /></label></div><label className="font-bold">Availability note<input name="availability" placeholder="e.g. I can arrive promptly" className="mt-2 min-h-12 w-full rounded-xl border px-4" /></label><input type="hidden" name="token" value={token} />{error && <p role="alert" className="rounded-xl bg-red-50 p-3 text-sm font-bold text-red-800">We couldn’t send your offer. Please try again.</p>}<button className="min-h-12 rounded-xl bg-[#23a955] font-black text-[#061b3f]">Send offer</button></form>}</section>; }

function ProviderConversations({ conversations }: { conversations: Array<{ id: string; providerName: string; providerPhotoUrl: string | null; latestBody: string | null; latestAt: string; hasQuote: boolean }> }) {
  return <section className="mt-8 border-t border-[#e9edf1] pt-7"><div className="flex items-baseline justify-between gap-4"><h2 className="text-2xl font-black">Provider conversations <span className="text-[#657089]">({conversations.length})</span></h2></div><div className="mt-4 grid gap-3">{conversations.map((conversation) => <article key={conversation.id} className="rounded-2xl border border-[#e7ebef] p-4"><div className="flex items-center gap-3">{conversation.providerPhotoUrl ? <img src={conversation.providerPhotoUrl} alt="" className="h-11 w-11 shrink-0 rounded-full object-cover" /> : <div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-full bg-[#eef8f1] text-lg font-black text-[#167d3c]" aria-hidden="true">{conversation.providerName.slice(0, 1).toUpperCase()}</div>}<div className="min-w-0"><p className="font-black">{conversation.providerName}</p><p className="text-xs font-bold text-[#657089]">{conversation.hasQuote ? "Quote sent" : "Question before quote"}</p></div></div>{conversation.latestBody && <p className="mt-3 line-clamp-2 text-sm text-[#39465b]">{conversation.latestBody}</p>}<div className="mt-3 flex items-center justify-between gap-3"><time dateTime={conversation.latestAt} className="text-xs font-bold text-[#657089]">{new Date(conversation.latestAt).toLocaleDateString("en-GB", { day: "numeric", month: "short" })}</time><Link href={`/messages/${conversation.id}`} className="font-black text-[#167d3c]">View conversation →</Link></div></article>)}</div></section>;
}

function Offers({ quotes, token, jobId, conversationByProvider, selectedQuoteId, paymentLocked, completed }: { quotes: Array<{ id: string; amount_pence: number; availability_text?: string | null; message?: string | null; status: string; provider_id?: string | null; bidder_user_id?: string | null; marketplace_providers?: { display_name?: string; business_name?: string; profile_photo_url?: string | null } | { display_name?: string; business_name?: string; profile_photo_url?: string | null }[] | null }>; token: string; jobId: string; conversationByProvider: Map<string | null, string>; selectedQuoteId: string | null; paymentLocked: boolean; completed: boolean }) {
  return <section id="quotes" className="mt-8 border-t border-[#e9edf1] pt-7">
    <div className="flex items-baseline justify-between gap-4"><h2 className="text-2xl font-black">Quotes <span className="text-[#657089]">({quotes.length})</span></h2></div>
    {!quotes.filter((quote) => quote.id !== selectedQuoteId).length ? <div className="mt-4 border-t border-[#edf0f3] pt-5"><p className="font-black">{selectedQuoteId ? "Your selected provider is shown above." : "No quotes yet"}</p><p className="mt-1 text-sm text-[#657089]">{selectedQuoteId ? "You can view other offers here if you need to change provider before payment." : "Providers can ask questions before sending a quote."}</p></div> : <div className="mt-4 grid gap-3">{quotes.filter((quote) => quote.id !== selectedQuoteId).map((quote) => {
      const profile = Array.isArray(quote.marketplace_providers) ? quote.marketplace_providers[0] : quote.marketplace_providers;
      const providerId = getMarketplaceQuoteProviderId(quote);
      const name = profile?.business_name || profile?.display_name || "Local provider";
      const conversationId = providerId ? conversationByProvider.get(providerId) : undefined;
      const selected = quote.id === selectedQuoteId;
      const unavailable = ["withdrawn", "expired"].includes(quote.status) || (quote.status === "declined" && (paymentLocked || !selectedQuoteId));
      return <article key={quote.id} className="rounded-2xl border border-[#e7ebef] bg-white p-5 shadow-[0_2px_12px_rgba(6,27,63,0.03)]">
        <div className="flex items-start justify-between gap-4">
          <div className="flex min-w-0 items-center gap-3">
            <ProviderAvatar src={profile?.profile_photo_url} name={name} className="h-11 w-11" />
            <div className="min-w-0">{providerId ? <Link href={`/providers/${providerId}?job=${jobId}&offer=${quote.id}&returnTo=/jobs/${token}`} className="font-black text-[#061b3f]">{name}</Link> : <p className="font-black">{name}</p>}{quote.availability_text && <p className="mt-1 text-sm text-[#526078]">{quote.availability_text}</p>}</div>
          </div>
          <p className="shrink-0 text-2xl font-black">{formatMarketplaceAmount(quote.amount_pence)}</p>
        </div>
        {quote.message && <p className="mt-4 rounded-xl bg-[#f7f9fb] p-3 text-sm leading-6 text-[#39465b]">{quote.message}</p>}
        <div className="mt-4 flex flex-wrap gap-3">{providerId && (conversationId ? <Link href={`/messages/${conversationId}`} className="inline-flex min-h-11 items-center rounded-xl border border-[#167d3c] px-4 font-black text-[#167d3c]">Message</Link> : <form action={startCustomerConversation}><input type="hidden" name="token" value={token} /><input type="hidden" name="providerId" value={providerId} /><button className="inline-flex min-h-11 items-center rounded-xl border border-[#167d3c] px-4 font-black text-[#167d3c]">Message</button></form>)}{selected ? <span className="inline-flex min-h-11 items-center rounded-xl bg-[#eef8f1] px-5 font-black text-[#167d3c]">{completed ? "Completed" : paymentLocked ? "Booked provider · Paid" : "Selected · Awaiting payment"}</span> : !unavailable && <form action={selectedQuoteId ? changeMarketplaceProvider : chooseMarketplaceQuote}><input type="hidden" name="token" value={token} /><input type="hidden" name="quoteId" value={quote.id} /><input type="hidden" name="returnTo" value={`/jobs/${token}`} /><button disabled={paymentLocked} className="min-h-11 rounded-xl bg-[#061b3f] px-5 font-black text-white disabled:cursor-not-allowed disabled:opacity-50">{selectedQuoteId ? `Change to ${name}` : `Choose ${name}`} · {formatMarketplaceAmount(quote.amount_pence)}</button></form>}{unavailable && <span className="inline-flex min-h-11 items-center rounded-xl bg-[#f7f8fa] px-5 font-black text-[#657089]">No longer available</span>}</div>
      </article>;
    })}</div>}
  </section>;
}

function formatJobLabel(key: string) {
  const labels: Record<string, string> = { cleanType: "Cleaning type", propertyType: "Property type", wasteRemoval: "Waste removal", bedrooms: "Bedrooms", bathrooms: "Bathrooms", extras: "Extras", access: "Access details", urgency: "Timing" };
  return labels[key] || key.replace(/([a-z])([A-Z])/g, "$1 $2").replaceAll("_", " ").replaceAll("-", " ").replace(/\b\w/g, (letter) => letter.toUpperCase());
}

function formatJobValue(value: unknown) {
  if (Array.isArray(value)) return value.join(", ");
  if (typeof value === "object" && value !== null) return Object.values(value as Record<string, unknown>).join(", ");
  return String(value).replaceAll("_", " ").replaceAll("-", " ");
}

function JobDetails({ data, photos, selected, paymentStatus, lifecycle }: { data: { service: string; service_subtype: string | null; pricing_answers: unknown; postcode: string; requested_timing: string | null; optional_note: string | null; budget_amount: number | null }; photos: Array<{ id: string; url: string | null }>; selected: boolean; paymentStatus?: string | null; lifecycle?: ReturnType<typeof getCustomerJobLifecycleState> | null }) {
  const answers = data.pricing_answers && typeof data.pricing_answers === "object"
    ? Object.entries(data.pricing_answers as Record<string, unknown>).filter(([key, value]) => {
        const normalized = key.toLowerCase().replaceAll("-", "_");
        return value !== null && value !== "" && !["service", "service_subtype", "postcode", "location", "requested_timing", "timing", "when", "budget"].includes(normalized);
      })
    : [];
  return <section className="mt-9 border-t border-[#edf0f3] pt-7">
    <h2 className="text-xl font-black">Job information</h2>
    {answers.length > 0 && <div className="mt-4 flex flex-wrap items-baseline gap-x-2 gap-y-1 text-sm"><span className="text-[#526078]">{formatJobLabel(answers[0][0])} {formatJobValue(answers[0][1])}</span></div>}
    <p className="mt-2 text-sm font-bold text-[#526078]">{paymentStatus === "paid" ? (lifecycle === "awaiting_customer_completion" ? "Awaiting your completion confirmation" : lifecycle === "completion_issue_reported" ? "Completion paused while the issue is reviewed" : lifecycle === "completed" ? "Job completed" : "Booking confirmed") : data.budget_amount == null ? (selected ? "Payment required" : "Open to offers") : `Budget · £${Number(data.budget_amount).toFixed(2).replace(/\\.00$/, "")}`}</p>
    {answers.length > 1 && <dl className="mt-4 grid gap-2 sm:grid-cols-2">{answers.slice(1).map(([key, value]) => <div key={key} className="flex justify-between gap-4 border-b border-[#f0f2f5] py-2 text-sm"><dt className="text-[#657089]">{formatJobLabel(key)}</dt><dd className="text-right font-bold">{formatJobValue(value)}</dd></div>)}</dl>}
    {data.optional_note && <div className="mt-4"><p className="text-sm font-black">Additional details</p><p className="mt-1 whitespace-pre-wrap text-sm leading-6 text-[#39465b]">{data.optional_note}</p></div>}
    {photos.some((photo) => photo.url) && <div className="mt-5"><p className="text-sm font-black">Photos</p><div className="mt-2 grid grid-cols-3 gap-2">{photos.filter((photo): photo is { id: string; url: string } => !!photo.url).map((photo) => <a key={photo.id} href={photo.url} target="_blank" rel="noreferrer"><img src={photo.url} alt="Job attachment" className="aspect-square w-full rounded-xl object-cover" /></a>)}</div></div>}
  </section>;
}

function Detail({ offerCount, createdAt }: { offerCount: number; createdAt?: string }) {
  const waiting = offerCount === 0;
  return <section className="mt-7 rounded-2xl border border-[#cfe9d7] bg-[#f5fbf6] p-5 sm:p-6">
    <p className="text-xs font-black uppercase tracking-[.14em] text-[#167d3c]">{waiting ? "Finding local people" : `${offerCount} ${offerCount === 1 ? "quote" : "quotes"} received`}</p>
    <p className="mt-2 text-lg font-black">{waiting ? "Waiting for quotes" : "You have people ready to help"}</p>
    <p className="mt-1 text-sm leading-6 text-[#526078]">{waiting ? <>{"Your job is live. We'll let you know when someone sends a quote."}</> : "Compare their price, availability and message before choosing."}</p>
    {createdAt && <p className="mt-3 text-xs font-bold text-[#526078]">Posted {relativePostedAt(createdAt)}</p>}
  </section>;
}
