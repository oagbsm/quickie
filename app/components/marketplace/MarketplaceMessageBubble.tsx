import PromoteContentButton from "@/app/messages/PromoteContentButton";

type MessageAttachment = { id: string; url: string | null; fileName?: string | null };

export default function MarketplaceMessageBubble({ body, isMine, attachments, createdAt, messageId, canPromote, promotedMessage, promotedAttachments }: { body?: string | null; isMine: boolean; attachments?: MessageAttachment[]; createdAt: string; messageId?: string; canPromote?: boolean; promotedMessage?: boolean; promotedAttachments?: string[] }) {
  const hasBody = Boolean(body?.trim());
  return <div className={`max-w-[82%] min-w-0 overflow-hidden break-words rounded-2xl p-3 text-sm leading-6 ${isMine ? "ml-auto bg-[#061b3f] text-white" : "bg-[#eef8f1]"}`}>{hasBody && <>{<p className="whitespace-pre-wrap break-words">{body}</p>}{canPromote && isMine && messageId && <PromoteContentButton kind="detail" messageId={messageId} added={promotedMessage} />}</>}{attachments?.filter((attachment) => attachment.url).map((attachment) => <div key={attachment.id} className="mt-2"><a href={attachment.url || undefined} target="_blank" rel="noreferrer" className="block max-w-full"><img src={attachment.url || ""} alt={attachment.fileName || "Conversation attachment"} className="max-h-48 max-w-full rounded-xl object-contain" /></a>{canPromote && isMine && messageId && <PromoteContentButton kind="photo" messageId={messageId} attachmentId={attachment.id} added={promotedAttachments?.includes(attachment.id)} />}</div>)}<time className="mt-1.5 block text-[10px] leading-4 text-[#8190a3] opacity-80">{new Intl.DateTimeFormat("en-GB", { dateStyle: "medium", timeStyle: "short" }).format(new Date(createdAt))}</time></div>;
}
