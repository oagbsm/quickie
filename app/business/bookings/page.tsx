import Link from "next/link";import {requireBusinessUser} from "@/lib/business/auth";export default async function Page(){const{supabase,accountId}=await requireBusinessUser();const{data}=await supabase.from("business_bookings").select("id,service,scheduled_start,status,price_pence,payment_status,properties(nickname)").eq("account_id",accountId).order("scheduled_start",{ascending:false});return <div><div className="flex items-end justify-between"><div><h1 className="text-3xl font-black">Bookings</h1><p className="mt-1 text-[#657089]">Track every visit from request to completion.</p></div><Link href="/business/bookings/new" className="rounded-xl bg-[#079448] px-4 py-3 font-black text-white">New booking</Link></div><div className="mt-6 grid gap-3">{data?.length?data.map((j:any)=><Link href={`/business/bookings/${j.id}`} key={j.id} className="grid gap-2 rounded-2xl border bg-white p-5 sm:grid-cols-[1fr_auto] sm:items-center"><div><p className="font-black">{j.properties?.nickname||"Property"} · {j.service.replaceAll("_"," ")}</p><p className="mt-1 text-sm text-[#657089]">{new Date(j.scheduled_start).toLocaleString("en-GB",{dateStyle:"medium",timeStyle:"short"})}</p></div><span className="w-fit rounded-full bg-[#edf7f1] px-3 py-1 text-xs font-black text-[#08783f]">{j.status.replaceAll("_"," ")}</span></Link>):<div className="rounded-2xl border border-dashed bg-white p-10 text-center"><p className="font-black">No bookings yet</p></div>}</div></div>}
