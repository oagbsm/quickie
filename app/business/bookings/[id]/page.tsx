import Link from "next/link";import {notFound} from "next/navigation";import {requireBusinessUser} from "@/lib/business/auth";
const steps=["Requested","Confirmed","Cleaner assigned","In progress","Completed","Approved or issue reported","Paid"];
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const{supabase,accountId}=await requireBusinessUser();const{data:j}=await supabase.from("business_bookings").select("*,properties(nickname,address_line_1,city,postcode)").eq("id",id).eq("account_id",accountId).maybeSingle();if(!j)notFound();return <div className="mx-auto max-w-4xl"><Link href="/business/bookings" className="text-sm font-black text-[#657089]">← Bookings</Link><div className="mt-4 rounded-2xl bg-[#061a3d] p-6 text-white"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-sm font-bold text-[#4bd35f]">{j.status.replaceAll("_"," ").toUpperCase()}</p><h1 className="mt-2 text-3xl font-black">{j.properties?.nickname}</h1><p className="mt-1 text-white/65">{j.service.replaceAll("_"," ")} · {new Date(j.scheduled_start).toLocaleString("en-GB",{dateStyle:"full",timeStyle:"short"})}</p></div>{j.price_pence!=null&&<p className="text-2xl font-black">£{(j.price_pence/100).toFixed(2)}</p>}</div></div><section className="mt-6 rounded-2xl border bg-white p-6"><h2 className="text-xl font-black">Job timeline</h2><ol className="mt-5 grid gap-4 sm:grid-cols-4">{steps.map((s,i)=><li key={s} className="flex gap-3 text-sm font-bold"><span className={`grid h-7 w-7 shrink-0 place-items-center rounded-full ${i===0?"bg-[#079448] text-white":"bg-[#eef1f5]"}`}>{i+1}</span>{s}</li>)}</ol></section><section className="mt-6 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl border bg-white p-5"><h2 className="font-black">Requirements</h2><p className="mt-2 whitespace-pre-wrap text-sm text-[#657089]">{j.requirements||"No additional requirements."}</p></div><div className="rounded-2xl border bg-white p-5"><h2 className="font-black">Completion evidence</h2><p className="mt-2 text-sm text-[#657089]">{j.completed_at?`Completed ${new Date(j.completed_at).toLocaleString("en-GB")}.`:"Completion details and protected photos will appear here after the job."}</p></div></section></div>}
