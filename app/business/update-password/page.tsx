"use client";import {FormEvent,useState} from "react";import {createSupabaseBrowserClient} from "@/lib/supabase/browser";import {useRouter} from "next/navigation";export default function Page(){const router=useRouter();const[m,setM]=useState("");async function go(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),p=String(f.get("password"));if(p.length<10){setM("Use at least 10 characters.");return}const{error}=await createSupabaseBrowserClient().auth.updateUser({password:p});if(error){setM(error.message);return}router.push("/business/dashboard")}return <main className="grid min-h-screen place-items-center bg-[#061a3d] p-5"><form onSubmit={go} className="grid w-full max-w-md gap-5 rounded-3xl bg-white p-8"><h1 className="text-3xl font-black">Choose a new password</h1><label className="font-bold">New password<input name="password" type="password" minLength={10} required className="mt-2 w-full rounded-xl border p-3"/></label>{m&&<p>{m}</p>}<button className="rounded-xl bg-[#079448] p-3 font-black text-white">Update password</button></form></main>}
