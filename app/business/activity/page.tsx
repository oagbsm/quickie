import { requireBusinessUser } from "@/lib/business/auth";
export default async function Page() { const { supabase, accountId } = await requireBusinessUser(); const { data } = await supabase.from("activity_events").select("id,event_type,description,created_at,work_item_id,properties(nickname),workers(display_name)").eq("account_id",accountId).order("created_at",{ascending:false}).limit(100); return <div className="mx-auto max-w-4xl"><p className="text-sm font-extrabold text-[#2d67b2]">AUDIT TRAIL</p><h1 className="mt-1 text-3xl font-extrabold tracking-[-.03em] sm:text-4xl">Activity</h1><p className="mt-2 text-[#657089]">A permanent history of operational changes across your workspace.</p><ol className="mt-7 border-l-2 border-[#dfe4eb] pl-6">{data?.length ? data.map(event => <li key={event.id} className="relative pb-7 before:absolute before:-left-[31px] before:top-1 before:h-3 before:w-3 before:rounded-full before:border-2 before:border-white before:bg-[#2d67b2]"><p className="font-extrabold">{event.description}</p><p className="mt-1 text-sm text-[#657089]">{new Intl.DateTimeFormat("en-GB",{dateStyle:"medium",timeStyle:"short"}).format(new Date(event.created_at))}</p></li>) : <li className="text-[#657089]">Activity will appear as you add properties and coordinate turnovers.</li>}</ol></div> }
