import Link from "next/link";import {requireBusinessUser} from "@/lib/business/auth";import {setPropertyStatus} from "../actions";
export default async function Page(){const{supabase,accountId}=await requireBusinessUser();const{data}=await supabase.from("properties").select("id,nickname,address_line_1,city,postcode,property_type,status").eq("account_id",accountId).order("created_at",{ascending:false});return <div><div className="flex items-end justify-between gap-3"><div><h1 className="text-3xl font-black">Properties</h1><p className="mt-1 text-[#657089]">Manage access and cleaning for every property.</p></div><Link href="/business/properties/new" className="rounded-xl bg-[#079448] px-4 py-3 font-black text-white">Add property</Link></div><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{data?.length?data.map(p=><article key={p.id} className={`rounded-2xl border bg-white p-5 ${p.status==="archived"?"opacity-65":""}`}><div className="flex justify-between gap-3"><div><h2 className="text-lg font-black">{p.nickname}</h2><p className="mt-1 text-sm font-semibold text-[#657089]">{p.address_line_1}, {p.city}, {p.postcode}</p></div><span className="h-fit rounded-full bg-[#f1f3f6] px-2.5 py-1 text-xs font-black">{p.status}</span></div><div className="mt-5 flex gap-2"><Link href={`/business/properties/${p.id}`} className="rounded-lg border px-3 py-2 text-sm font-black">View</Link>{p.status==="active"&&<Link href={`/business/bookings/new?property=${p.id}`} className="rounded-lg bg-[#edf7f1] px-3 py-2 text-sm font-black text-[#079448]">Book a service</Link>}<form action={setPropertyStatus}><input type="hidden" name="id" value={p.id}/><input type="hidden" name="status" value={p.status==="active"?"archived":"active"}/><button className="rounded-lg px-2 py-2 text-sm font-bold text-[#657089]">{p.status==="active"?"Archive":"Restore"}</button></form></div></article>):<div className="rounded-2xl border border-dashed bg-white p-10 text-center md:col-span-2"><p className="font-black">No properties yet</p><Link href="/business/properties/new" className="mt-3 inline-block font-black text-[#079448]">Add your first property</Link></div>}</div></div>}
