import Link from "next/link";
import { signOut } from "../actions";
export default async function Page({searchParams}:{searchParams:Promise<{ref?:string}>}){const{ref}=await searchParams;return <main className="grid min-h-screen place-items-center bg-[#f4f6f9] p-5"><section className="w-full max-w-lg rounded-3xl bg-white p-8 shadow-sm"><p className="text-sm font-black text-amber-700">WORKSPACE SETUP</p><h1 className="mt-2 text-3xl font-black">We couldn’t finish setting up your workspace.</h1><p className="mt-4 leading-7 text-[#657089]">Your account is signed in, but we could not create your business workspace.</p><p className="mt-4 rounded-xl bg-[#f4f6f9] p-3 text-sm font-bold">Reference: {ref||"WS-UNKNOWN"}</p><div className="mt-6 flex flex-col gap-3 sm:flex-row"><Link href="/business/continue" className="rounded-xl bg-[#079448] px-5 py-3 text-center font-black text-white">Retry setup</Link><form action={signOut}><button className="w-full rounded-xl border px-5 py-3 font-black">Sign out</button></form></div></section></main>}
