"use client";import{FormEvent,useState}from"react";import{useRouter,useSearchParams}from"next/navigation";import{createSupabaseBrowserClient}from"@/lib/supabase/browser";import{safeInternalNextPath}from"@/lib/app-url";export default function AdminLoginForm(){const router=useRouter(),params=useSearchParams();const[pending,setPending]=useState(false),[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setMessage("");const f=new FormData(e.currentTarget),{error}=await createSupabaseBrowserClient().auth.signInWithPassword({email:String(f.get("email")),password:String(f.get("password"))});if(error){setMessage("Email or password is incorrect.");setPending(false);return}router.replace(safeInternalNextPath(params.get("next"),"/admin"));router.refresh()}const c="mt-2 w-full rounded-xl border border-[#d9e0e8] px-4 py-3 outline-none focus:border-[#079448] focus:ring-4 focus:ring-[#079448]/10";return <form onSubmit={submit} className="grid gap-5 rounded-3xl bg-white p-8 shadow-xl"><div><p className="text-sm font-black text-[#079448]">QUICKOLA OPERATIONS</p><h1 className="mt-2 text-3xl font-black">Admin sign in</h1><p className="mt-2 text-sm text-[#657089]">Use an authorised Quickola admin account.</p></div><label className="font-bold">Email<input name="email" type="email" autoComplete="email" required className={c}/></label><label className="font-bold">Password<input name="password" type="password" autoComplete="current-password" required className={c}/></label>{message&&<p role="alert" className="rounded-xl bg-red-50 p-3 text-sm font-bold text-red-800">{message}</p>}<button disabled={pending} className="min-h-11 rounded-xl bg-[#071638] px-5 font-black text-white disabled:opacity-60">{pending?"Signing in…":"Sign in"}</button></form>}
