import { requireCleanerUser } from "@/lib/cleaner/auth";
import TurnoverCards from "../TurnoverCards";
import CleanerNavigation from "../CleanerNavigation";
import { selectRelevantCleanerJobs } from "@/lib/cleaner/lifecycle";
export default async function Page() { const { supabase, workerId } = await requireCleanerUser(); const { data } = await supabase.from("work_items").select("id,property_public_name,property_general_area,turnover_date,access_start_at,window_end_at,estimated_duration_minutes,status,properties(nickname,postcode),assignments!inner(status,worker_id)").eq("assignments.worker_id", workerId).in("assignments.status", ["pending", "accepted"]).not("status", "in", "(draft,unassigned,declined,cancelled,ready,evidence_submitted)").order("access_start_at"); const jobs = selectRelevantCleanerJobs(data || []); return <><h1 className="text-2xl font-extrabold sm:text-3xl">Today</h1>{jobs.primary ? <><TurnoverCards items={[jobs.primary]} empty="" />{jobs.secondary.length > 0 && <><h2 className="mt-6 text-lg font-extrabold">Other assigned jobs</h2><TurnoverCards items={jobs.secondary} empty="" /></>}</> : <div className="mt-4"><TurnoverCards items={[]} empty="No active or upcoming assignments." /></div>}<CleanerNavigation /></>; }
