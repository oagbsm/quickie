import type { Metadata } from "next";
import Link from "next/link";
import MarketplaceHeader from "@/app/components/marketplace/MarketplaceHeader";
import { marketplaceLocations, marketplaceServices } from "@/app/data/marketplace";
export const metadata: Metadata = { title: "Local service locations | Quickola", description: "Explore the locations and service areas in Quickola's local marketplace architecture.", alternates: { canonical: "/locations" } };
export default function LocationsPage() { return <main className="min-h-screen bg-[#f7f8fa] text-[#061b3f]"><MarketplaceHeader /><section className="bg-[#061b3f] px-5 py-16 text-white sm:px-8"><div className="mx-auto max-w-[1120px]"><p className="text-xs font-black uppercase tracking-[.16em] text-[#23dc63]">LOCATIONS</p><h1 className="mt-4 text-5xl font-black tracking-[-.06em]">Local services, area by area.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-white/70">Quickola’s marketplace coverage will grow carefully, with useful local pages rather than empty location lists.</p></div></section><section className="px-5 py-12 sm:px-8"><div className="mx-auto grid max-w-[1120px] gap-4 md:grid-cols-3">{marketplaceLocations.map((location) => <article key={location.slug} className="rounded-2xl bg-white p-6 ring-1 ring-[#e9edf1]"><h2 className="text-2xl font-black">{location.name}</h2><p className="mt-3 text-sm leading-6 text-[#707b8d]">{location.description}</p><div className="mt-5 grid gap-2">{marketplaceServices.slice(0, 4).map((service) => <Link key={service.slug} href={`/services/${service.slug}/${location.slug}`} className="text-sm font-extrabold text-[#167d3c]">{service.name} →</Link>)}</div></article>)}</div></section></main>; }
