import { Pressable, Text, View } from "react-native";
import { colors } from "@/lib/theme";

export function QuickActionItem({ icon, title, description, onPress, comingSoon = false }: { icon: string; title: string; description: string; onPress?: () => void; comingSoon?: boolean }) {
  return <Pressable accessibilityRole={onPress && !comingSoon ? "button" : undefined} accessibilityLabel={comingSoon ? `${title}, coming soon` : title} disabled={comingSoon} onPress={onPress} style={({ pressed }) => [item, pressed && !comingSoon && { backgroundColor: "#f5f8fc" }, comingSoon && { opacity: 0.62 }]}><View style={iconBox}><Text style={iconText}>{icon}</Text></View><View style={{ flex: 1 }}><Text style={titleText}>{title}</Text><Text style={descriptionText}>{description}</Text></View>{comingSoon ? <Text style={soon}>Coming soon</Text> : <Text style={chevron}>›</Text>}</Pressable>;
}
const item = { minHeight: 66, flexDirection: "row" as const, alignItems: "center" as const, gap: 12, paddingVertical: 9, paddingHorizontal: 10, borderRadius: 12 }; const iconBox = { width: 40, height: 40, borderRadius: 12, backgroundColor: "#eaf2fc", alignItems: "center" as const, justifyContent: "center" as const }; const iconText = { color: colors.blue, fontSize: 20, fontWeight: "800" as const }; const titleText = { color: colors.ink, fontSize: 15, fontWeight: "800" as const }; const descriptionText = { color: colors.muted, fontSize: 12, marginTop: 3 }; const soon = { color: colors.muted, fontSize: 11, fontWeight: "800" as const }; const chevron = { color: colors.blue, fontSize: 25, fontWeight: "300" as const };
